import 'package:flutter_test/flutter_test.dart';

import 'package:SpeechMaster/app/shared/service/watson_service.dart';

void main() {
  WatsonService service;

  setUp(() {
//    service = WatsonService();
  });
//
  group('WatsonService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<WatsonService>());
//    });
//
  });
}
