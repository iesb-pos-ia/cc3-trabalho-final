import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:SpeechMaster/app/modules/index/index/index_controller.dart';
import 'package:SpeechMaster/app/modules/index/index_module.dart';

void main() {
  initModule(IndexModule());
  // IndexController index;
  //
  setUp(() {
    //     index = IndexModule.to.get<IndexController>();
  });

  group('IndexController Test', () {
    //   test("First Test", () {
    //     expect(index, isInstanceOf<IndexController>());
    //   });

    //   test("Set Value", () {
    //     expect(index.value, equals(0));
    //     index.increment();
    //     expect(index.value, equals(1));
    //   });
  });
}
