import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:SpeechMaster/app/modules/speak/index/index_controller.dart';
import 'package:SpeechMaster/app/modules/speak/speak_module.dart';

void main() {
  initModule(SpeakModule());
  // IndexController index;
  //
  setUp(() {
    //     index = SpeakModule.to.get<IndexController>();
  });

  group('IndexController Test', () {
    //   test("First Test", () {
    //     expect(index, isInstanceOf<IndexController>());
    //   });

    //   test("Set Value", () {
    //     expect(index.value, equals(0));
    //     index.increment();
    //     expect(index.value, equals(1));
    //   });
  });
}
