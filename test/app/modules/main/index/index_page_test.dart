import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:SpeechMaster/app/modules/main/index/index_page.dart';

void main() {
  testWidgets('IndexPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(IndexPage(title: 'Index')));
    //  final titleFinder = find.text('Index');
    //  expect(titleFinder, findsOneWidget);
  });
}
